import GlobalHeader from '@/components/GlobalHeader/GlobalHeader'
import './GlobalHeader.scss'

export default function SnappingFeedHeader(props) {
    return (
        <GlobalHeader className="snapping_feed_header" {...props} />
    )
}